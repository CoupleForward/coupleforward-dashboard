# Lab Handoff → 2026-08-13

State of the REWIRE Lab after the two-day build sprint (overnight 08-11→12 plus the live iteration day 08-13 with Christian). Written for the next session and for Christian's own review. Canonical in the repo at `docs/`, mirrored to iCloud `Couple Forward/03 Programs/REWIRE LAB/`.

## Where things stand, in one paragraph

The Lab is a real, working product on localhost: magic-link-only sign-in, solo-or-couple onboarding with names and birthdays, the fully synced Huddle with history, a privacy-first daily check-in with drawn-face scales and streaks, two-lane journaling (shared and private), live metric drill-downs on every card, the 4,000 Weeks card with interactive rings and life-in-weeks dot grids, working breathwork, data export, and the open app shelf behind an "Access Apps" drawer. Christian is signed in with his real account (couple created, together since March 11, 1994, birthday set); Amber's invite is pending. Everything is committed and pushed through `main`. The database is the shared Supabase project with five new migrations applied supervised and verified. Nothing is deployed yet, and deploy is now the single decision gating four features.

## Applied to the live database (all supervised, all verified)

1. `20260811090000_lab_tighten_rls_updates` → closed the couple-takeover hole (WITH CHECK everywhere, display_name-only column grant).
2. `20260812120000_lab_solo_and_names` → `couples.is_solo`, `couple_invites.invited_name`.
3. `20260812150000_lab_daily_checkins` → daily check-ins, author-private rows, `lab_daily_summary` RPC (blended connection + gap, never the partner's raw numbers).
4. `20260812160000_lab_journal_privacy` → `journal_entries.visibility`, read policy verified live: private entries visible only to their author.
5. `20260812170000_lab_member_birthdays` → `couple_members.birthday`, grant re-issued for display_name + birthday.

Anon RLS suite: passing after every apply. The security advisor warnings that remain are pre-existing and expected (GraphQL schema visibility with RLS intact, intentional SECURITY DEFINER RPCs).

## Product decisions Christian made this sprint (binding)

→ **Auth: magic-link only** for now; Google sign-in later via Supabase provider. Request and open the link in the SAME browser (PKCE).
→ **Daily check-in**: three questions (in myself / my day / us), answered with five DRAWN faces (eyes-nose-mouth, no circles, no emoji; fifth face ecstatic open-mouth), weighted 2/4/6/8/10. Raw answers are private to their author at the database level; partners share the blended connection score, the gap band, checked-in status, and streaks (yours / partner's / together). Weekly quiz is separate and still awaits his red pen (draft in `docs/plan/05`).
→ **Journaling is two-lane**: shared-with-us or just-for-me, chosen at writing time, RLS-enforced.
→ **4,000 Weeks card**: Together/You tabs; interactive double rings (gold = so far, cream = left, tap a ring to see its total; number only, no captions); dot calendar under the rings; detail grids show each life in weeks with exactly two origin marks: red = the week the couple began, cream = birth week. No recurring markers. Weeks-left-together = the tightest KNOWN partner horizon. OPEN: keep the hard 4,000-week frame vs actuarial life expectancy for individual weeks-left (he saw 1,116 at age 55 and it read as low; he never answered "keep 4,000 or actuarial").
→ **Dashboard layout (standing rule, also in repo CLAUDE.md)**: two columns; compact trio (streak, connection, journey) over the video window; right rail = prompt, check-in, daily lines, upcoming, journal, somatic; NEW features go in a full-width section underneath all columns, never appended to a column. "Access Apps" labeled pill opens the slide-out shelf.
→ **Mobile-first is a standing rule**: every surface built and verified at phone size (saved to persistent memory; it caught a whole-app-down bundle bug on day one).
→ **The Lab is two-anchor**: rhythm membership (Huddle + apps + tracking) with the REWIRE journey (no "Jr.") as the invited layer; $47 base, journey as 90-day subscription step-up (anchor prices open).
→ **Mentors for journal prompts**: PD four (Aurelius, Seneca, Epictetus, Plato) can be quoted; Maya Angelou is NOT public domain (corrected); his ranked list still pending. Untethered Soul / No Bad Parts PDFs incoming as design sources; ship only his-voice originals.
→ **Roadmap visuals**: the Inner Compass road/compass JPGs (saved at `public/brand/`) are the TEMPLATE; the product version gets rebuilt as an SVG scene (unfolding road, tappable nodes, phone-reflowable); his hand drawing decides the path shape.

## Deploy is now the bottleneck (one decision, four features)

Blocked until the Lab lives on a real domain: (1) invite emails / production SMTP (Amber saw no email; invites are in-app by design until then), (2) calendar subscription feeds (phones cannot subscribe to localhost), (3) Amber or anyone using it from their own device, (4) push notification groundwork. Plus the pre-deploy code blocker: the week/day-key timezone divergence documented in `src/lib/lab/week.ts` (server UTC vs viewer-local Monday; decide fixed-timezone keys vs client-derived keys BEFORE deploying).

## On Christian's desk (nothing here blocks the build queue below)

→ Amber: sign in at localhost:3000 with ambercharette@me.com (on this Mac), accept the invite, add her birthday.
→ Red-pen the weekly quiz questions + bless the `compass_reports` attach-flow shape (`docs/plan/05`).
→ Ranked mentor list + the Angelou call (inspired-by originals or omit).
→ Meditation recordings (or scripts) for the guided player.
→ The hand-drawn node map + his two ideas (SLOTS 1-2), the movement dictionary pass (docs/plan/01) — still the critical path to the journey engine.
→ Decisions: 4,000 vs actuarial; deploy domain; support/deletion contact address; timeline + solo-vs-couples-first (SLOTS 3-4).

## Build queue (next session picks up here)

1. **Mentor journal prompts architecture** (task 13; started, nothing committed): mentor registry + prompt banks, three journaling modes (open / weekly CF prompt / mentor POV), PD content first.
2. **Guided meditation player** (task 14): generalize the breathwork pacer; audio slot for his recordings.
3. **Retroactive mobile pass** (task 15) over every surface, per the standing rule.
4. **Profile photos** in the header couple graphic (task 16): avatars storage bucket + upload on /account (next supervised apply).
5. **Calendar feed** (task 18, deploy-gated) + weekly quiz build once red-penned.
6. Roadmap SVG scene prototype once the node drawing arrives.

## Where everything lives

→ Repo `~/Dev/coupleforward-dashboard`, all pushed to `CoupleForward/coupleforward-dashboard` main. Run: `npm run dev` → localhost:3000; `npm run typecheck`; `npm run test:rls`.
→ Planning set `docs/plan/00..05` (REWIRE Lab plan, journey architecture, build sequence, video shoot list, sales video, instruments spec), mirrored to iCloud `03 Programs/REWIRE LAB/`.
→ Brand road/compass art: `public/brand/*.JPG` (from the INNER_COMPASS_ASSESTS repo).
→ Session memory updated: `coupleforward-dashboard-lab` (state), `lab-mobile-first-rule` (standing rule), `somatic-resources-third-party` (source-material policy).
→ Lint baseline: 5 pre-existing react-hooks errors in original files (huddle, welcome, UpcomingCard) + 2 warnings in the membership stub; everything new is clean.
